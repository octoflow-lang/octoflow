# sort (L2)
sort — In-place sorting algorithms.

## Functions
insertion_sort(arr: array) → array
  Sort via insertion sort (stable, in-place)
bubble_sort(arr: array) → array
  Sort via bubble sort (stable, in-place)
