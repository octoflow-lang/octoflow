# digits (L2)
terminal/digits — ASCII digit art renderer (pixel brightness to digits 0-9)

## Functions
digits_render(r: array, g: array, b: array, w: int, h: int) → string
  Render RGB image as ASCII digit art string for any terminal
