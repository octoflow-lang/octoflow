# encoding (L2)
crypto/encoding — Base64 and hex encoding/decoding wrappers (prefer builtins directly)

## Functions
to_base64(s: string) → string
  Encode string to Base64
from_base64(s: string) → string
  Decode Base64 string
to_hex(s: string) → string
  Encode string to hex
from_hex(s: string) → string
  Decode hex string
