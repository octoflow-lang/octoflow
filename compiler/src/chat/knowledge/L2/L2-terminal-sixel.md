# sixel (L2)
terminal/sixel — Sixel graphics renderer (6 vertical pixels per char, 216-color palette)

## Functions
sixel_render(r: array, g: array, b: array, w: int, h: int) → string
  Render RGB image as Sixel graphics escape sequence
