# math_ext (L2)
stats/math_ext — Extended math utilities

## Functions
factorial(n: int) → float | permutations(n: int, r: int) → float
gcd | lcm(a: int, b: int) → int — GCD / LCM
is_prime(n: int) → float — 1.0/0.0 | fibonacci(n: int) → float
power_mod(base: int, exp: int, mod: int) → int — Modular exp
sigmoid(x: float) → float | relu(x: float) → float — Activations
linspace(start: float, stop: float, n: int) → array — N evenly spaced
arange(start: float, stop: float, step: float) → array — Range by step
dot_product(a: array, b: array) → float — Dot product
magnitude(v: array) → float | normalize_vec(v: array) → array — Norm/unit vec
cross_product_3d(a: array, b: array) → array — 3D cross product
