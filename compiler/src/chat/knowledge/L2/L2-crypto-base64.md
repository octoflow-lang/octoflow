# base64 (L2)
encoding/base64 — Pure .flow Base64 encoder/decoder (standard alphabet, = padding)

## Functions
b64_encode(s: string) → string
  Encode string to Base64
b64_decode(s: string) → string
  Decode Base64 string to original
