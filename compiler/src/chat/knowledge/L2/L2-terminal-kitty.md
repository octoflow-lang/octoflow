# kitty (L2)
terminal/kitty — Kitty Graphics Protocol renderer (Base64 via SPIR-V compute)

## Functions
kitty_render(r: array, g: array, b: array, w: int, h: int) → string
  Render RGB image as Kitty graphics protocol escape sequence
