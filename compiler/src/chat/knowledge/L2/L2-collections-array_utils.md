# array_utils (L2)
array_utils — Array search, sort, aggregate, slice, unique, flatten, zip.

## Functions
arr_contains(arr: array, val: any) → int
  Check if array contains value
index_of(arr: array, val: any) → int
  First index of value or -1
arr_sum(arr: array) → float
  Sum of elements
arr_avg(arr: array) → float
  Mean of elements
arr_min(arr: array) → float
  Minimum element
arr_max(arr: array) → float
  Maximum element
swap(arr: array, i: int, j: int) → array
  Swap two elements
reverse(arr: array) → array
  Reverse array
fill(val: any, n: int) → array
  Create array of n copies
binary_search(arr: array, val: any) → int
  Binary search sorted array
arr_sort(arr: array) → array
  Sort array ascending
arr_slice(arr: array, start: int, end: int) → array
  Slice sub-array
arr_unique(arr: array) → array
  Remove duplicates
arr_flatten(arr: array) → array
  Flatten nested arrays
arr_zip(a: array, b: array) → array
  Zip two arrays into pairs
arr_count(arr: array, val: any) → int
  Count occurrences of value
