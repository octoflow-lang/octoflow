# halfblock (L2)
terminal/halfblock — ANSI truecolor halfblock renderer (2 pixels per cell, 256M colors)

## Functions
halfblock_render(r: array, g: array, b: array, w: int, h: int) → string
  Render RGB image as ANSI halfblock escape sequences
