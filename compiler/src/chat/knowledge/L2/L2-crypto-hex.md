# hex (L2)
encoding/hex — Pure .flow hex encoder/decoder at byte-array level

## Functions
bytes_to_hex(arr: array) → string
  Encode byte array to hex string
hex_to_bytes(s: string) → array
  Decode hex string to byte array
