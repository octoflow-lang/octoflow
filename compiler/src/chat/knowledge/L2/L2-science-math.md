# math (L2)
math — Scalar math utilities.

## Functions
min_of(a: float, b: float) → float
  Return smaller value
max_of(a: float, b: float) → float
  Return larger value
clamp_val(x: float, lo: float, hi: float) → float
  Clamp x to [lo, hi]
lerp(a: float, b: float, t: float) → float
  Linear interpolation
map_range(x: float, a1: float, a2: float, b1: float, b2: float) → float
  Map value from range [a1,a2] to [b1,b2]
sign(x: float) → float
  Sign of x (-1, 0, or 1)
deg_to_rad(d: float) → float
  Degrees to radians
rad_to_deg(r: float) → float
  Radians to degrees
gcd(a: int, b: int) → int
  Greatest common divisor
lcm(a: int, b: int) → int
  Least common multiple
