# memory (L2)
sys/memory — GPU memory info.

## Functions
gpu_mem_total() → int
  Total GPU memory in bytes
gpu_mem_used() → int
  Used GPU memory in bytes
gpu_device_name() → string
  GPU device name string
