[package]
name = "octoflow-vulkan"
version = "1.3.0"
edition = "2021"
description = "Vulkan compute runtime for OctoFlow"

[dependencies]
# Zero external crates — raw Vulkan via vk_sys.rs
# Linked via build.rs → vulkan-1.lib (Windows) / libvulkan.so (Linux)

[dev-dependencies]
