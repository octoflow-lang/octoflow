// hello_selfhosted.flow — Test self-hosted compilation
print("Hello from eval.flow compiler!")

let x = 42.0
print("The answer is {x}")

let mut sum = 0.0
let mut i = 1.0
while i <= 10.0
  sum = sum + i
  i = i + 1.0
end

print("Sum 1..10 = {sum}")
