// Run decomposed streaming text generation
// Prerequisites: run run_decompose.flow first to create per-layer .bin files
// Usage: octoflow run stdlib/llm/run_generate_decomposed.flow --allow-read --allow-ffi

use "generate_decomposed"

let model_path = "C:/FlowGPU/models/qwen2.5-1.5b.gguf"
let decomp_dir = "C:/FlowGPU/models/qwen-decomp"
let prompt = "Write a Python function to check if a number is prime."

let _r = run_generate_decomposed(model_path, decomp_dir, prompt)
